{
    "name": "@knnect/module_fed_sample",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "clean": "lerna run clean --stream",
        "bootstrap": "npm ci && lerna bootstrap",
        "build:prod": "lerna run build:prod --stream",
        "start": "lerna run start --parallel"
    },
    "dependencies": {
        "react": "^17.0.2",
        "react-dom": "^17.0.2"
    },
    "devDependencies": {
        "lerna": "^3.22.1"
    }
}
